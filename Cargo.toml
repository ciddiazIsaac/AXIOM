[workspace]
members = [
    "axiom-core", "pdp-server", "axiom-p2p"
]
exclude = [
    "regorus-local",
]
resolver = "2"

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
panic = "abort"
strip = true
